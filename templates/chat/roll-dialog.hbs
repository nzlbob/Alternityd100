<form>

  
<div class="form-group">
    <label>{{stepflavor}}</label>
    
  </div>

  <div class="form-group">
    <label>Base Roll</label>
    <input type="text" name="formula" value="{{formula}}" disabled/>
  </div>
  <!--<div class="form-group">
    <label>Difficulty Steps</label>
    <input type="text" name="bonus" value="" placeholder="e.g. +1d4"/>
  </div>-->
    <div class="form-group">
    <label>Difficulty Steps</label>
    <select name="dstep">
      {{selectOptions difficultySteps selected=dstep}}
    </select>
  </div>
  <div class="form-group">
    <label>RollMode</label>

        <select name="rollMode">
                    {{ selectOptions rollModes selected=mode localize=true }}
                </select>


  <!--  <select name="rollMode">
      {[{#select rollMode}}
      {[{#each rollModes as |label mode|}}
    <option value="{{mode}}"><span>{{localize label}}</span></option>
      {[{/each}}
      {[{/select}}
    </select>
-->
  </div>
   <div class="form-group">

    </div>
    
</form>
